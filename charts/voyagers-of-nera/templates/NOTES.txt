Voyagers of Nera Dedicated Server has been deployed!

Server Name: {{ .Values.voyagers.hostSettings.displayName }}
Max Players: {{ .Values.voyagers.hostSettings.maxPlayers }}

{{- if eq .Values.voyagers.service.type "NodePort" }}
The server is exposed via NodePort. Connect using:
  - Game Port: {{ .Values.voyagers.service.nodePort | default "auto-assigned" }}

To get the assigned node port if auto-assigned:
  kubectl get svc {{ include "voyagers-of-nera.fullname" . }} -n {{ .Release.Namespace }} -o jsonpath='{.spec.ports[0].nodePort}'
{{- end }}

The server will appear in the in-game server browser once it's fully started.

To view logs:
  kubectl logs -f deployment/{{ include "voyagers-of-nera.fullname" . }} -n {{ .Release.Namespace }}

Configuration files are stored at:
  {{ .Values.persistence.data.mountPath }}/BoatGame/Saved/PersistedData/CustomConfig/
